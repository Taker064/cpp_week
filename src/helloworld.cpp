#include <iostream>  //ヘッダファイルの読み込み.hは必要ない

using namespace std; //標準名前空間の使用.これがないとcoutがstd::coutになる

int main(){
    cout << "HelloWorld." << endl;//cout内の変数郡の使用.coutが出力,endlは改行<<はストリーム
    return 0;
}